<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Slot5"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Slot5">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity for Overview -->
        <activity
            android:name=".OverviewActivity"
            android:exported="true"
            android:label="Overview Activity" />

        <!-- Activity for Definition -->
        <activity
            android:name=".DefinitionActivity"
            android:exported="true"
            android:label="Definition Activity" />

        <!-- Activity for Lifecycle -->
        <activity
            android:name=".LifecycleActivity"
            android:exported="true"
            android:label="Lifecycle Activity" />
    </application>
</manifest>
